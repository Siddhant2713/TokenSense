# URL of the Express API server.
# Only relevant if you run the server with OPENAI_API_KEY set (optional server-key tier).
# In pure BYOK mode the dashboard works without a running server.
VITE_API_URL=http://localhost:3001
